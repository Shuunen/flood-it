{
  "name": "flood-it",
  "version": "0.2.0",
  "description": "A flood it game",
  "main": "game.js",
  "scripts": {
    "start": "json-server --watch db.json  --static . --port 8088",
    "start-db": "json-server --watch db.json --port 8088"
  },
  "author": "Shuunen",
  "license": "AGPL-3.0",
  "devDependencies": {
    "eslint": "^3.17.1",
    "jscs": "^3.0.7"
  },
  "dependencies": {
    "json-server": "^0.9.6"
  }
}